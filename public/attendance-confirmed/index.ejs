<% if(partial == false) { %> <% include ../header %> <% } %>

    <% title= 'Verify Email'; %>
    <% subtitle = ''; %>
    <% secondary_subtitle = ''; %>

    <div view class="animatable" id="view-check-email">
      <div class="container">
        <h1>Attendance Confirmed!</h1>
        <p>
          Thank you for confirming your attendance with us! If you've confirmed that you will be attending
          RevoutionUC please be sure to email us at <a href="mailto:info@revolutionuc.com">info@revolutionuc.com</a> if you can no longer make it.
        </p>
      </div>
    </div>

<% if(partial == false) { %> <% include ../footer %> <% } %>
