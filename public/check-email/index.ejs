<% if(partial == false) { %> <% include ../header %> <% } %>

    <% title= 'Verify Email'; %>
    <% subtitle = ''; %>
    <% secondary_subtitle = ''; %>

    <div view class="animatable" id="view-check-email">
      <div class="container">
        <h1>Almost Done!</h1>
        <p>To complete registration, please check your email and click the link we sent you.</p>
      </div>
    </div>

<% if(partial == false) { %> <% include ../footer %> <% } %>
